#include<stdio.h>
int main()
{
    int a[100],n,i;
    printf("how many numbers you want to enter=");
    scanf("%d",&n);
    printf("enter the numbers=\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a[i]>large)
        {
            large=a[i];
            lpos=i;
        }
    }
    for(i=0;i<n;i++)
    {
        if(a[i]<small)
        {
            small=a[i];
            spos=i;
        }
    }
    a[lpos]=small;
    a[spos]=large;
    for(i=0;i<n;i++)
    {
        printf("%d\t",a[i]);
    }
    return 0;
}