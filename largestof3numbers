#include<stdio.h>
int main()
{
   int a,b,c,largest;
   printf("enter three numbers to find the largest of them\n");
   scanf("%d%d%d",&a,&b,&c);
   largest=(a>b)?((a>c)?a:c):((b>c)?b:c);
   printf("largest of the entered numbers is=%d\n",largest);
   return 0;
}